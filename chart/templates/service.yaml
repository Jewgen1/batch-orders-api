apiVersion: v1
kind: Service
metadata:
  name: batch-orders-api
  namespace: batch-orders
  labels:
    app: batch-orders-api
spec:
  selector:
    app: batch-orders-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: {{ .Values.service.type }}
